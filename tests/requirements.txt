# Python dependencies for fixture generation only — these are not needed
# to run `npm test`, since the test suite reads pre-generated fixtures
# under tests/fixtures/ that are committed to the repo.
#
# Install with:
#     pip install -r tests/requirements.txt
#
# Versions below are the ones used to generate the committed fixtures.
# Loosen (e.g., librosa>=0.10) if exact installs are unavailable on your
# platform, but verify the parity tests still pass after regeneration.

librosa==0.11.0
numpy==2.4.4
scipy==1.17.1
