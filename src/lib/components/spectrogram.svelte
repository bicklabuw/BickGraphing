<!-- <script>
    import { createFFmpeg } from "@ffmpeg/ffmpeg";
  
    const ffmpeg = createFFmpeg({
      log: true,
      corePath: "/ffmpeg-core/ffmpeg-core.js", // Use the local file instead of the CDN
    });
  
    async function loadFFmpeg() {
      await ffmpeg.load();
      console.log("FFmpeg is running offline!");
    }
  
    onMount(loadFFmpeg);
  </script> -->

<!-- <h1></h1> -->
